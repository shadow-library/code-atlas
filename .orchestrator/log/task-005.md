# Task 005 — Logging setup (structlog)

**Status:** done
**Sub-agent:** general-purpose (agentId a92b6ae4bf7f892a9)

## Files changed
- pyproject.toml (modified — added `structlog` runtime dep)
- src/code_atlas/utils/__init__.py (new)
- src/code_atlas/utils/logging.py (new)
- tests/unit/utils/test_logging.py (new)
- uv.lock (regenerated by `uv sync --extra dev`)

## Sub-agent response (verbatim)

### Summary
Add structlog-based logging utilities. New `code_atlas.utils` package exposes `configure_logging` and `get_logger`. `configure_logging` is idempotent, supports JSON (prod) and console (dev) renderers, ISO UTC timestamps, level filtering via `make_filtering_bound_logger`, and stream redirection. Adds `structlog>=24.1,<26.0` as the sole runtime dependency. Six unit tests cover JSON parseability, console readability, ISO timestamps, bound-logger shape, level filtering, and idempotent reconfigure.

### State update
Task 005 done. Add `structlog` dep. New `utils/` pkg: `configure_logging` (JSON/console, idempotent, ISO UTC, filter wrapper, PrintLoggerFactory→stream) + `get_logger` (cast to `BoundLogger`). 6 tests cover JSON parse, console human, ISO ts, bound shape, level filter, reconfigure swap. Strict mypy clean via `cast` + `Processor` annotation.

### Next task
Task 006 — likely path/token utility helpers under `code_atlas.utils` (token counting, repo path normalization) per architecture excerpt. Confirm next from `TASKS.md` before starting.

## Apply notes

- HTML-entity decoding again: `-&gt;` → `->`; `&gt;=24.1,&lt;26.0` → `>=24.1,<26.0`.
- Brief amended pre-dispatch to add `pyproject.toml` to task 005's file list (needed to declare `structlog` as a runtime dep). TASKS.md updated accordingly.
- `uv sync --extra dev` re-resolved deps; structlog 25.5.0 landed.
- Initial `uv sync` (without `--extra dev`) stripped dev tools — re-ran with `--extra dev`. Lockfile is consistent again.
- Acceptance verified locally:
  - `uv run ruff format --check src tests` → OK (6 files formatted)
  - `uv run ruff check src tests` → All checks passed
  - `uv run mypy src/code_atlas` → Success (4 files, strict)
  - `uv run pytest tests/ -q` → 14 passed (8 existing + 6 new)
- Sub-agent's Next-task suggestion (token/path helpers) doesn't match the plan (next is task 006: Config). Ignored.
