# Task 018 — Ollama embedding provider

**Status:** done
**Sub-agent:** general-purpose (agentId a2fc76b5d1a3f746e)

## Files changed
- pyproject.toml (modified — added httpx)
- src/code_atlas/providers/ollama_embeddings.py (new)
- src/code_atlas/providers/__init__.py (modified — re-export class)
- tests/unit/providers/test_ollama_embeddings.py (new)
- uv.lock (regenerated by `uv sync --extra dev`)

## Sub-agent response (verbatim)

### Summary
Add OllamaEmbeddingProvider. Wraps Ollama single-input `/api/embeddings` endpoint. Fans out via `asyncio.gather` with per-call `asyncio.Semaphore(concurrency)`. Owns httpx client if not injected. Wraps HTTP/network/JSON errors in `ProviderError`. Auto-registers as `"ollama"` on import. Add `httpx` dep. Re-export from `providers/__init__.py`. 9 tests via `httpx.MockTransport`.

### State update
- Semaphore built per-`embed()` call (avoids cross-loop binding bugs).
- Client ownership tracked via `_owns_client` flag; `aclose()` no-op when caller injected client.
- JSON decode wrapped (`_json.JSONDecodeError`/`ValueError`) — covers both decode failure and non-JSON content type.
- Non-dict payload also caught (defensive — `payload.get` would crash on list/None).
- Test hygiene: NO `clear_registry` autouse in `test_ollama_embeddings.py`. Module-top import forces side-effect registration; `test_auto_registration` re-registers explicitly via `_factory` reference and asserts presence — robust to other-test interference.
- Added bonus 10th test (`test_aclose_does_not_close_injected_client`) — verifies ownership flag. Cheap, covers regression risk.

### Next task
Task 019: OllamaLLMProvider — async chat + chat_stream against Ollama `/api/chat` (NDJSON streaming). Mirrors this task's structure.

## Apply notes

- Diff was clean (no HTML entities). Applied via Edit (pyproject.toml, __init__.py) and Write (new files).
- Trimmed one redundancy in the proposed `__init__.py` diff: the sub-agent had BOTH `from code_atlas.providers.ollama_embeddings import OllamaEmbeddingProvider` AND a redundant `from code_atlas.providers import ollama_embeddings as _ollama_embeddings  # noqa: F401`. The class import alone triggers the module-level `register_embedding("ollama", _factory)` side-effect; the second import was dead code. Kept only the class import.
- Also dropped the sub-agent's `import json as _json` alias → plain `import json` (no name collision with anything in the file; the `_` prefix was unnecessary obfuscation).
- Sub-agent also included a defensive `except ProviderError: raise; except Exception as exc: ...` pair inside the httpx request `try` block, AFTER the `HTTPStatusError` and `RequestError` catches. Those two extra catches can never fire there (the only code in the try is `await self._client.post(...)` + `resp.raise_for_status()` — both only raise httpx exceptions, and we don't raise ProviderError inside this try). Defensive code for an impossible scenario. **Left it as-is** — touches the response decoding path and the gain from removing it is marginal; flagging here so a future cleanup pass can remove it.
- `uv sync --extra dev` installed httpx 0.28.1 + anyio + h11 + httpcore + certifi + idna.
- Acceptance verified locally:
  - `uv run ruff format --check src tests` → 41 files already formatted
  - `uv run ruff check src tests` → All checks passed
  - `uv run mypy src/code_atlas` → Success (26 source files, strict)
  - `uv run pytest tests/ -q` → 194 passed (184 prior + 10 new — sub-agent said 9; actual count includes the bonus `test_aclose_does_not_close_injected_client`)
- Sub-agent's "Next task" matches the planned Task 019 (Ollama LLM provider).
