# Task 014 — Vector store (LanceDB + Protocol)

**Status:** done
**Sub-agent:** general-purpose (agentId a09806396e992ec2c)

## Files changed
- pyproject.toml (modified — added lancedb)
- src/code_atlas/indexing/__init__.py (modified — re-export VectorStore/VectorItem/LanceVectorStore)
- src/code_atlas/indexing/vector_store.py (new)
- tests/unit/indexing/test_vector_store.py (new)
- uv.lock (regenerated by `uv sync --extra dev`)

## Sub-agent response (verbatim)

### Summary
Implement LanceDB-backed vector store with Protocol, frozen VectorItem model, idempotent merge_insert upserts, cosine search with repo_id filter, persistence, and full error wrapping per locked design.

### State update
- LanceVectorStore live, sync API, callers wrap to_thread
- VectorItem frozen pydantic in vector_store.py (not domain/)
- merge_insert keyed on chunk_id, idempotent
- repo_id filter only; single-quote rejected (no SQL escape logic)
- delete_repo counts BEFORE delete (LanceDB rowcount unreliable)
- lancedb + pyarrow imported via importlib for mypy strict
- cosine score = 1.0 - _distance, can exceed 1 by float epsilon

### Next task
Task 015: implement symbol_graph.py (NetworkX) per architecture — last indexing primitive before indexer.py orchestrator can land.

## Apply notes

- Diff was clean (no HTML entities to decode).
- Applied via Edit/Write — new files for vector_store.py + test; small edits for pyproject.toml and __init__.py.
- `uv sync --extra dev` installed lancedb 0.33.0 + pyarrow 24.0.0 + 9 other transitive deps.
- Two post-write fixes:
  1. Ruff RUF046: `int(len(df))` in count() pandas fallback — `len()` already returns int. Dropped the redundant `int()` cast.
  2. Tried swapping `table_names()` → `list_tables()` to silence a lancedb 0.33 DeprecationWarning. Broke all 16 tests with `cannot use 'tuple' as a set element (unhashable type: 'list')` — lancedb 0.33's `list_tables()` returns `(namespace, name)` tuples containing unhashable lists, NOT a string list. Reverted to `table_names()`; deprecation warning is noise, swap is NOT a drop-in. Logged in STATE.md to prevent future-me chasing it again.
- Acceptance verified locally:
  - `uv run ruff format --check src tests` → 30 files already formatted
  - `uv run ruff check src tests` → All checks passed
  - `uv run mypy src/code_atlas` → Success (19 files, strict)
  - `uv run pytest tests/ -q` → 149 passed (133 prior + 16 new)
- Sub-agent's "Next task" matches the planned Task 015 (NetworkX symbol graph).
