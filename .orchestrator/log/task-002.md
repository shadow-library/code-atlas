# Task 002 — Dev tooling (ruff, mypy, pytest, pre-commit, editorconfig)

**Status:** done
**Sub-agent:** general-purpose (agentId a96cac24e9b09e232)

## Files changed
- pyproject.toml (modified — added dev extras + tool sections)
- .pre-commit-config.yaml (new)
- .editorconfig (new)
- uv.lock (new — generated by `uv sync`)

## Sub-agent response (verbatim)

### Summary
Added dev tooling configuration: pinned `dev` extras in `pyproject.toml`, configured ruff (lint + format), mypy strict, pytest, and coverage. Created `.pre-commit-config.yaml` with ruff, generic hygiene hooks, and a local mypy hook backed by `uv run`. Created `.editorconfig` with per-file-type indent and line-length rules. Preserved existing `[tool.uv]` and `[tool.hatch...]` sections.

### State update
- Dev extras pinned. ruff, mypy, pytest, pytest-cov, pytest-asyncio, pre-commit.
- Ruff configured. line-length 120. py311 target. select E/F/I/UP/B/SIM/RUF.
- Ruff format: double quotes, space indent, lf endings.
- Mypy strict. files src/code_atlas. warn_unused_ignores + warn_redundant_casts on.
- Pytest: testpaths tests, pythonpath src, asyncio auto, markers slow/network/requires_ollama.
- Coverage: branch on, source code_atlas, excludes pragma/TYPE_CHECKING/NotImplementedError.
- Pre-commit: ruff-format, ruff --fix, hygiene hooks, local mypy via uv run.
- Editorconfig: utf-8/lf root. py 4-space cap 120. yaml/json/md/toml 2-space. Makefile tabs.

### Next task
Task 003 — scaffold `src/code_atlas/` package skeleton (cli entrypoint, typed `__init__`, ports/adapters dirs) so ruff/mypy/pytest configs have real surface to lint.

## Apply notes

- Diff was content-correct but transcript pipeline HTML-encoded `>` / `<` in version pins (`&gt;`, `&lt;`); decoded before transcribe.
- `git apply` rejected the patch at the empty `[tool.ruff.lint.per-file-ignores]` section boundary (corrupt-patch error at line 69). Files transcribed via Edit/Write — content matches the diff.
- Acceptance verified locally:
  - `uv sync --extra dev` resolves all deps.
  - `uv run ruff format --check src` → FMT OK.
  - `uv run ruff check src` → All checks passed.
  - `uv run mypy src/code_atlas` → Success.
- The sub-agent's Next task suggestion conflicts with the planned task 003 (CI pipeline). Ignored — package structure is built incrementally across tasks 004–007.
