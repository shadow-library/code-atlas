# Task 015 — Symbol graph (NetworkX MultiDiGraph)

**Status:** done
**Sub-agent:** general-purpose (agentId a82c31531c65dd545)

## Files changed
- pyproject.toml (modified — added `networkx>=3.2,<4.0`)
- src/code_atlas/indexing/__init__.py (modified — re-exports `EdgeKind`, `SymbolGraph`)
- src/code_atlas/indexing/symbol_graph.py (new)
- tests/unit/indexing/test_symbol_graph.py (new)
- uv.lock (regenerated by `uv sync --extra dev`)

## Sub-agent response (verbatim)

### Summary
Add `SymbolGraph` over `networkx.MultiDiGraph` with typed edges (calls/imports/defines/contained_in), gzipped JSON node-link persistence, and deterministic caller/callee queries. Node id = `(path, name)` tuple stored as node attr `node_id` to survive JSON tuple->list round-trip; full `Symbol` payload kept as `symbol_data`. Wires re-exports, adds `networkx` runtime dep, ships 16 unit tests including the 5-symbol/6-edge acceptance round-trip.

### State update
- SymbolGraph live in `code_atlas.indexing` — `nx.MultiDiGraph`, sync, edges keyed by EdgeKind literal (calls/imports/defines/contained_in)
- Node id = `(path, name)` tuple; persisted attr `node_id=[path,name]` so JSON list->tuple coercion on load works (chose attr-roundtrip over `\x00` stringify)
- Persistence = gzip(json.dumps(node_link_data, sort_keys=True), level=9); `nx.node_link_data(..., edges="edges")` to dodge nx 3.x FutureWarning
- `callers`/`callees` filter on `key=="calls"` only, return deduped sorted by `(path,name)`; missing symbol = `[]`
- Added `networkx>=3.2,<4.0` to pyproject runtime deps — main agent must `uv sync` before tests/mypy
- Acceptance test (5 nodes, 6 edges across 4 kinds) ships in `test_symbol_graph.py::test_acceptance_five_symbols_six_edges_roundtrip`

### Next task
Wire `SymbolGraph` into the ingest pipeline (`ingest_repo`) so chunker-extracted defines/contained_in edges get persisted alongside metadata/lexical/vector stores — currently graph is built but no producer feeds it.

## Apply notes

- Diff was clean (no HTML entities to decode). Applied via Edit (pyproject.toml, __init__.py) + Write (symbol_graph.py, test_symbol_graph.py).
- `uv sync --extra dev` installed networkx 3.6.1 (~2MB).
- **Post-write fix (mypy strict, expected)**: Sub-agent used `import networkx as nx` and typed attributes as `nx.MultiDiGraph`. mypy strict flagged two errors:
  1. `Library stubs not installed for "networkx"` — no shipped stubs; mypy hint says `types-networkx`.
  2. `has_node` returns Any → declared bool.
  Followed the project's established precedent (vector_store, parser) for stub-free libs: switched to `importlib.import_module("networkx")` typed as `Any`, both at module level and on `self._g`. Added `bool(...)` wrap on `has_node` return. Two-line change, consistent with vector_store + tree-sitter-language-pack precedent.
- Acceptance verified locally:
  - `uv run ruff format --check src tests` → 32 files already formatted
  - `uv run ruff check src tests` → All checks passed
  - `uv run mypy src/code_atlas` → Success (20 source files, strict)
  - `uv run pytest tests/ -q` → 165 passed (149 prior + 16 new)
- Sub-agent's "Next task" suggestion (`SymbolGraph` → ingest pipeline) doesn't match the planned Task 016 (indexer orchestrator), which is the natural next step per TASKS.md and is broader than just wiring graph producers. Not adopting the suggestion verbatim; Task 016 already covers the wiring in a more complete shape (metadata + lexical + vector + graph in one pass).
