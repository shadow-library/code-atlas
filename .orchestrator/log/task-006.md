# Task 006 — Config (pydantic-settings + YAML)

**Status:** done
**Sub-agent:** general-purpose (agentId ad44607ee0fcf6759)

## Files changed
- pyproject.toml (modified — runtime deps + dev types-pyyaml)
- src/code_atlas/config/__init__.py (new)
- src/code_atlas/config/settings.py (new)
- config/default.yaml (new)
- .env.example (new)
- tests/unit/config/test_settings.py (new)
- uv.lock (regenerated by `uv sync --extra dev`)

## Sub-agent response (verbatim)

### Summary
Add `code_atlas.config` package implementing `Settings` (pydantic-settings) with nested groups, YAML + .env + env-var precedence (init > env > .env > yaml), and a `load_settings` testable entry point. Adds runtime deps (`pydantic`, `pydantic-settings`, `pyyaml`) and `types-pyyaml` for mypy strict. Ships `config/default.yaml`, `.env.example`, and 7 unit tests.

### State update
- config pkg at `src/code_atlas/config/`.
- deps: pydantic>=2.8, pydantic-settings>=2.4, pyyaml>=6.0; dev gains types-pyyaml.
- Settings holds 7 nested BaseModel groups.
- env prefix CODE_ATLAS_, nested delim __, default yaml config/default.yaml, default env .env, extra="ignore".
- settings_customise_sources order = init, env, dotenv, yaml, secrets.
- load_settings(yaml_path?, env_file?, **overrides) makes one-off Settings subclass when paths override.
- non-existent yaml/env paths silently empty.
- 7 tests pass: defaults, yaml override, env>yaml, dotenv layering, init>all, bad-type ValidationError, out-of-range ValidationError.

### Next task
007 — Domain types (pydantic frozen models: `CodeChunk`, `Symbol`, `RetrievalQuery`, `RetrievalResult`, `Citation`, `Answer`, `TokenUsage`) under `src/code_atlas/domain/` + round-trip tests.

## Apply notes

- HTML-entity decoding again across all comparison operators (`&gt;` → `>`, `&lt;` → `<`, `-&gt;` → `->`).
- Two post-write fixes needed:
  - Sub-agent's `settings_customise_sources` returned a single-line tuple (123 chars) over the 120 limit. `ruff format` auto-wrapped it.
  - Mypy strict rejected `SettingsConfigDict(**{**Settings.model_config, **overrides_cfg})` because `dict[str, Any]` can't be ** -expanded into a TypedDict. Changed to `cast("SettingsConfigDict", {**Settings.model_config, **overrides_cfg})` with `cast` added to imports.
- Acceptance verified locally:
  - `uv run ruff format --check src tests` → OK (9 files)
  - `uv run ruff check src tests` → All checks passed
  - `uv run mypy src/code_atlas` → Success (6 files, strict)
  - `uv run pytest tests/ -q` → 21 passed (14 prior + 7 new)
- Sub-agent's Next-task suggestion (Task 007 — Domain types) matches the planned next task.
